export interface CropData {
  time: Date;
  humidity: number;
  temperature: number;
  waterLevel: number;
  height: number;
  imageUrl: string;
}

// const getData = (limit: number): CropData[] => {

// };
